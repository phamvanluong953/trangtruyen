<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
  <style>
    body {
      background-color: #f0f2f5;
      margin: 0;
      padding: 0;
    }

    .forgot-wrapper {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .forgot-card {
      width: 100%;
      max-width: 400px;
      border: none;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      background-color: white;
    }

    .forgot-header {
      font-size: 24px;
      font-weight: bold;
      color: #e4bb8b;
      text-align: center;
      padding: 20px 0 10px 0;
    }

    .btn-send {
      background-color: #e4bb8b;
      border: none;
    }

    .btn-send:hover {
      background-color: #d9a96a;
    }

    .back-login {
      display: block;
      text-align: center;
      margin-top: 15px;
      font-size: 14px;
    }

    .back-login a {
      text-decoration: none;
      color: #1877f2;
    }

    .back-login a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
 <div class="container">
  <%- include('includes/navbar') %>

  <div class="forgot-wrapper">
    <div class="card forgot-card">
      <div class="forgot-header"><%= title %></div>
      <div class="card-body">
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-danger" role="alert">
            <%= error %>
          </div>
        <% } %>
        <% if (typeof success !== 'undefined' && success) { %>
          <div class="alert alert-success" role="alert">
            <%= success %>
          </div>
        <% } %>

        <form action="/quenmatkhau" method="post" class="needs-validation" novalidate>
          <div class="mb-3">
            <label for="Email" class="form-label">Nhập địa chỉ Email của bạn</label>
            <input type="email" class="form-control" id="Email" name="Email" placeholder="Email" required />
          </div>

          <button type="submit" class="btn btn-send w-100 text-white fw-bold">
            <i class="bi bi-envelope"></i> Gửi mật khẩu mới
          </button>

          <div class="back-login">
            <a href="/dangnhap"><i class="bi bi-arrow-left-circle"></i> Quay lại đăng nhập</a>
          </div>
        </form>
      </div>
    </div>
  </div>

  <%- include('includes/footer') %>
  </div>
  <%- include('includes/javascript') %>
</body>

</html>
