<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background: #f5f7fa;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      .cv-container {
        background: white;
        max-width: 700px;
        margin: 40px auto;
        padding: 30px 40px;
        box-shadow: 0 4px 12px rgb(0 0 0 / 0.1);
        border-radius: 8px;
      }

      .cv-header {
        border-bottom: 2px solid #e4bb8b;
        padding-bottom: 15px;
        margin-bottom: 30px;
      }

      .cv-header h1 {
        font-weight: 700;
        font-size: 2.4rem;
        color: #e4bb8b;
      }

      .cv-section-title {
        font-weight: 600;
        font-size: 1.2rem;
        color: #5c3a00;
        border-bottom: 1px solid #e4bb8b;
        margin-bottom: 10px;
        padding-bottom: 5px;
      }

      .cv-detail {
        font-size: 1rem;
        color: #4a3b1a;
      }

      .icon-check {
        color: #8c6c2f;
        margin-right: 5px;
      }

      .icon-cross {
        color: #b04e00;
        margin-right: 5px;
      }

      .btn-primary {
        background-color: #e4bb8b;
        border-color: #d6a960;
      }

      .btn-primary:hover {
        background-color: #d6a960;
        border-color: #c69c43;
      }

      .avatar {
        width: 120px;
        height: 120px;
        object-fit: cover;
        border-radius: 50%;
        border: 3px solid #e4bb8b;
        margin-bottom: 15px;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <%- include('includes/navbar') %>

      <div class="cv-container mt-4">
        <div class="cv-header text-center">
          <% if (taikhoan.HinhAnh) { %>
          <img
            src="/uploads/<%= taikhoan.HinhAnh %>"
            alt="Ảnh đại diện"
            class="avatar"
          />
          <% } else { %>
          <i
            class="bi bi-person-circle"
            style="font-size: 120px; color: #e4bb8b; margin-bottom: 15px"
          ></i>
          <% } %>

          <h1><%= taikhoan.HoVaTen %></h1>
          <p class="text-muted fs-5 mb-0">
            <i class="bi bi-envelope"></i> <%= taikhoan.Email %>
          </p>
        </div>

        <div class="cv-section">
          <div class="cv-section-title">Thông tin cá nhân</div>
          <p class="cv-detail">
            <strong>Quyền hạn:</strong> <%= taikhoan.QuyenHan %>
          </p>
          <p class="cv-detail">
            <strong>Kích hoạt:</strong>
            <% if (taikhoan.KichHoat == 1) { %>
            <i class="bi bi-check-circle icon-check"></i> Đã kích hoạt <% } else
            { %> <i class="bi bi-x-circle icon-cross"></i> Chưa kích hoạt <% }
            %>
          </p>
        </div>

        <div class="cv-section mt-4 text-center">
          <a
            href="/taikhoan/sua_user/<%= taikhoan._id %>"
            class="btn btn-primary btn-lg"
          >
            <i class="bi bi-pencil-square"></i> Chỉnh sửa thông tin
          </a>
        </div>
      </div>

      <%- include('includes/footer') %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
